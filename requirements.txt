# ============================================================
# requirements.txt  ·  CONTENEDOR DE STREAMLIT CLOUD (deploy)
# ------------------------------------------------------------
# Va en la RAIZ del repo y debe llamarse EXACTAMENTE requirements.txt.
# Es deliberadamente esbelto: la app NO carga modelos en memoria,
# los llama de forma remota via HuggingFace Inference API.
# Por eso NO incluye torch ni transformers (eso revienta la RAM ~1 GB).
# ============================================================

streamlit==1.57.0
pandas==2.3.3
numpy==2.3.4
yfinance==1.3.0
huggingface_hub==0.35.0      # cliente ligero de inferencia (InferenceClient)

# --- Incluir SOLO si la demo ejecuta M3/M4 en vivo. ---
# Si la demo lee resultados precomputados, comenta estas dos lineas.
pyportfolioopt==1.5.6
quantstats==0.0.64
matplotlib==3.10.7
