# ============================================================
# requirements-dev.txt  ·  ENTORNO LOCAL (notebooks, validacion, tests)
# ------------------------------------------------------------
# Streamlit Cloud IGNORA este archivo (no se llama requirements.txt).
# Sirve para reconstruir tu maquina local:
#     pip install -r requirements-dev.txt
# Aqui SI van torch, transformers y el pipeline NLP dual de M1.
# ============================================================

# --- Core ciencia de datos ---
pandas==2.3.3
numpy==2.3.4
scipy==1.17.0
scikit-learn==1.8.0
matplotlib==3.10.7
seaborn==0.13.2
pyarrow==23.0.0

# --- Datos de mercado y optimizacion ---
yfinance==1.3.0
pyportfolioopt==1.5.6
quantstats==0.0.64

# --- NLP local: modelos cargados en memoria (M1 completo) ---
transformers==4.57.6
torch==2.11.0
huggingface_hub==0.35.0
sentencepiece==0.2.1         # requerido por el tokenizer de opus-mt-es-en (Pipeline A)
sacremoses==0.1.1            # detokenizacion de los modelos Helsinki-NLP
pysentimiento==0.7.4         # RoBERTuito, Pipeline B del NLP dual

# --- Herramientas de desarrollo ---
pytest==9.0.3
ipykernel==7.1.0
nbconvert==7.17.1
streamlit==1.57.0            # para probar la app en local antes de desplegar
